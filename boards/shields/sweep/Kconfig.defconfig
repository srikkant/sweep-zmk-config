if SHIELD_SWEEP_DONGLE || SHIELD_SWEEP_LEFT || SHIELD_SWEEP_RIGHT

config ZMK_SPLIT
	default y

endif

if SHIELD_SWEEP_DONGLE

config ZMK_KEYBOARD_NAME
	default "srikkant Sweep"

config ZMK_SPLIT_ROLE_CENTRAL
	default y

endif

